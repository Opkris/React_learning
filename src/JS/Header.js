import React from "react";

 function Header () {



   return(
       <h1>Hello World</h1>
   )





    /* const firstName = "Kristoffer";
     const lastName = "Opdahl";

     const date = new Date();

     let hours = date.getHours();
     let timeOfDay;
     const styles = {
         fontSize: "19px"
     };
         if (hours < 12){
             timeOfDay = "Good Morning";
         styles.color = "red"
         }
         else if( hours >= 12 && hours <= 17){
             timeOfDay = " good afternoon";
             styles.color = " orange"
         }
         else{
             timeOfDay = "night";
             styles.color = "black"
         }


     return(
         <div>
            <header className={"navbar"}>This is a Header made by {`${firstName} ${lastName}`} !</header>
             <h2 style={styles}>{date.getHours() +"." + date.getMinutes() + ". \n" + timeOfDay}</h2>
         </div>
     )*/
}

export default Header
